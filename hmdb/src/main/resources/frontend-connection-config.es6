// Domain where the next.JS server is running - where to contact the frontend for HTML rendering in CS, and the Origin header when the API is contacted
export const FRONTEND_DOMAIN = "http://localhost:3000";
