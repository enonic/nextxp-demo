= Rendering basics
:toc: right
:imagesdir: media/
:sourcedir: ../src

In this chapter we will look into the basic rendering mechanisms and add some custom rendering to our app as well.

== Dev mode rendering

As you have seen during the setup of Next, each content item automatically maps to a page listing available props. These props are fetched by the so-called Enonic Adapter.

By defining custom queries and react components tailored for each content type, we can customize the props passed to the view, and which React template is actually applied.

== Task: Get person content

Let's build our first component, starting with a rendering of the "Person" content type:

NOTE: The full name of your content type should now be `tutorial.nxp:person`.
If you used a different name when creating the Enonic app, update the `.env` file in your Next.js app to reflect this.

. **Add a query** by creating the following file in your front-end application:
+
.src/components/queries/getPerson.ts
[source,TypeScript]
----
include::{sourcedir}/components/queries/getPerson.ts[]
----
+
This file defines a graphQL query 'getPerson' that will fetch data from the Enonic API.
+
NOTE: Query uses variables `siteKey`, `branch`, `project` and `path` that are automatically populated by the Enonic next.js adapter.
+
TIP: You may validate the query by copying it to the GraphQL playground.
Replace `APP_NAME_UNDERSCORED` with `tutorial_nxp` and set the query variables, try this for instance:
`{"path": "/hmdb/persons/brad-pittless", "project": "intro", "branch": "draft", "siteKey": "/hmdb"}`
+
. **Add the following imports** to the component mappings file:
+
.src/components/_mappings.ts
[source,JavaScript]
----
import getPerson from './queries/getPerson';
import {APP_NAME} from '@enonic/nextjs-adapter';
----
+
. Then **Register the query** in the same file:
+
.src/components/_mappings.ts
[source,JavaScript]
----
// Content type mappings
ComponentRegistry.addContentType(`${APP_NAME}:person`, {
    query: getPerson,
    view: PropsView
});
----
+
. **Check the result**
+
By visiting a person item (like `http://localhost:3000/persons/morgan-floatman[http://localhost:3000/persons/morgan-floatman^]`) you should now see data props listed in the view.
+
image:morgan-floatman-props.png[title="Page showing data props for Morgan Floatman content item",width=767px]

== Media URLs

The `getPerson` query requests the image with `${imageUrlQuery({scale: 'width(500)'})}`, a Next.XP helper that selects the URL object (`url`, `path`, `queryString` and the media components) of a scaled version of the image.
Its `path` is server-relative, so the view turns it into an absolute URL pointing at XP's media API with `imageUrl(photo.imageUrl)`.
The base URL comes from `ENONIC_MEDIA_CDN` and defaults to `ENONIC_API`, so no XP configuration is needed.

Every Guillotine URL field has such a pair: `imageUrlQuery()`/`imageUrl()`, `mediaUrlQuery()`/`mediaUrl()`, `attachmentUrlQuery()`/`attachmentUrl()` and `pageUrlQuery()`/`pageUrl()`.
The `pageUrl(data, meta)` function is the one to use for links between content items: it returns a relative URL prefixed with the current locale, except for the default locale which has no prefix.

== Task: Add person template

With fresh content available, it's time to make things shine:

. **Add a view** by creating the following file in your Next.js application
+
.src/components/views/Person.tsx
[source,TypeScript]
----
include::{sourcedir}/components/views/Person.tsx[]
----
+
This file defines a React view `Person` that will use the props from the query to render the person.
+
. **Register the Person view** by adding the following import to the component mappings:
+
.src/components/_mappings.ts
[source,JavaScript]
----
import Person from './views/Person';
----
+
And then update the component mapping you created earlier to use the new view:
+
.src/components/_mappings.ts
[source,JavaScript]
----
// Content type mappings
ComponentRegistry.addContentType(`${APP_NAME}:person`, {
    query: getPerson,
    view: Person
});
----
+
. **Check the result** - The page should automatically reload once the changes have been saved
+
image:morgan-floatman-render.png[title="Person rendering with query and view",width=768px]

The content type mapping applies to every `person` content item in the site, regardless of its location in the hierarchy.
Try looking at some other persons, e.g., http://localhost:3000/persons/uma-tieoffman[http://localhost:3000/persons/uma-tieoffman^]

== Task: Add custom header and footer

With some styling applied, the header and the footer will make the site more interesting visually. Next.js provides a handy solution to this problem.

Next.js layouts wrap every page below them. The starter has a root layout (`src/app/layout.tsx`), a locale layout and a content layout (`src/app/[locale]/[[...contentPath]]/layout.tsx`); by modifying the last one we can inject the same header and footer components into all content pages.

TIP: Sample header and footer views are already included in the starter under `src/components/views`, so all we need to do is put them to work.

Update `src/app/[locale]/[[...contentPath]]/layout.tsx` with the following changes:

. **Add these imports**
+
[source,TypeScript]
----
import Header from '../../../components/views/Header';
import Footer from '../../../components/views/Footer';
----
+
. And at the bottom of the file **replace the entire return function**:
+
[source,TypeScript]
----
return (
    <LocaleContextProvider locale={resolvedParams.locale}>
        <StaticContent condition={isEdit}>
            <main>{children}</main>
            <DraftModeIndicator label={I18n.localize('draft-mode')}/>
        </StaticContent>
    </LocaleContextProvider>
)
----
+
With this:
+
[source,TypeScript]
----
return (
    <LocaleContextProvider locale={resolvedParams.locale}>
        <StaticContent condition={isEdit}>
            <Header meta={meta} title={I18n.localize('title')} logoUrl="/images/xp-shield.svg"/>
            <main>{children}</main>
            <Footer/>
            <DraftModeIndicator label={I18n.localize('draft-mode')}/>
        </StaticContent>
    </LocaleContextProvider>
)
----

Morgan Floatman's page should now look like this

image:morgan-floatman-header-footer.png[title="Rendering with header and footer",width=768px]

== The common query

You may have noticed the `Common` props appearing in the various prop views - these originate from the following statement in the mappings:

.src/components/_mappings.ts
[source,TypeScript]
----
...

// Common query
ComponentRegistry.setCommonQuery([commonQuery, commonVariables]);

...
----

The result of this query is available across all components on the page, and may be useful if you want to fetch data for the page itself or to be used across multiple components. Feel free to adapt it to your requirements.

NOTE: If you do not need any common props, remove it from the mappings to optimize performance.

With the basic rendering completed, in the following chapter, you'll add <<preview#, preview support to Content Studio>>.

